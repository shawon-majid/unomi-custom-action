{
  "name": "unomi-test",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "web": "bun run web/serve.ts",
    "server": "bun run server/index.ts",
    "setup": "bash unomi/setup.sh",
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f unomi"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
